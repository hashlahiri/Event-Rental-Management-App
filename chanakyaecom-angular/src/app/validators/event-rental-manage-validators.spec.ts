import { EventRentalManageValidators } from './event-rental-manage-validators';

describe('EventRentalManageValidators', () => {
  it('should create an instance', () => {
    expect(new EventRentalManageValidators()).toBeTruthy();
  });
});
