import { AdminCustomRequests } from './admin-custom-requests';

describe('AdminCustomRequests', () => {
  it('should create an instance', () => {
    expect(new AdminCustomRequests()).toBeTruthy();
  });
});
