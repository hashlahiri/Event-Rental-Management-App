import { AdminOrderContent } from './admin-order-content';

describe('AdminOrderContent', () => {
  it('should create an instance', () => {
    expect(new AdminOrderContent()).toBeTruthy();
  });
});
