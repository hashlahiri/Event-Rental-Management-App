import { AdminViewerContact } from './admin-viewer-contact';

describe('AdminViewerContact', () => {
  it('should create an instance', () => {
    expect(new AdminViewerContact()).toBeTruthy();
  });
});
